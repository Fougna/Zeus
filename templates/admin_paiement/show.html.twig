{% extends 'base.html.twig' %}

{% block title %}ADMINISTRATION : Paiement{% endblock %}

{% block body %}
    <h1>Paiement</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>N° Identifiant</th>
                <td>{{ paiement.id }}</td>
            </tr>
            <tr>
                <th>Création</th>
                <td>{{ paiement.creation ? paiement.creation|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Session Stripe n°</th>
                <td>{{ paiement.stripeSessionId }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_paiement_index') }}">Retour à la liste</a>

    <a href="{{ path('admin_paiement_edit', {'id': paiement.id}) }}">Modifier</a>

    {{ include('admin_paiement/_delete_form.html.twig') }}
{% endblock %}