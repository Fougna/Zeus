<form method="post" action="{{ path('admin_paiement_delete', {'id': paiement.id}) }}" onsubmit="return confirm('Êtes-vous sûr(e) de supprimer cette entrée ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ paiement.id) }}">
    <button class="btn">Supprimer</button>
</form>