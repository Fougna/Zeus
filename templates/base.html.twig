<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <meta http-equiv="X-UA-Compatible" content="IE=edge">
        {# Intégration de la balise meta "viewport" pour activer la responsivité du site #}
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>
            {% block title %}
                ZEUS EX MACHINA : La Mythologie Grecque à portée de clic !
            {% endblock %}
        </title>
        {# Intégration Favicon #}
        <link rel="icon" href="{{ asset('images/Zeuslogo.png') }}">
        {# Run `composer require symfony/webpack-encore-bundle` to start using Symfony UX #}
        {% block stylesheets %}
            {# Intégration Bootstrap CSS #}
            <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
            {# Intégration Fontawesome CDN #}
            <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css" integrity="sha512-Fo3rlrZj/k7ujTnHg4CGR2D7kSs0v4LLanw2qksYuRlEzO+tcaEPQogQ0KaoGN26/zrn20ImR1DfuLWnOo7aBA==" crossorigin="anonymous" referrerpolicy="no-referrer" />
            {# Intégration fichier CSS #}
            <link rel="stylesheet" href="{{ asset('css/style.css') }}">
            {# Intégration CSS de w3schools #}
            <link rel="stylesheet" href="https://www.w3schools.com/w3css/4/w3.css">
            {# Intégration Google Fonts #}
            <link rel="preconnect" href="https://fonts.googleapis.com">
            <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
            <link href="https://fonts.googleapis.com/css2?family=Mohave:wght@300;500;700&display=swap" rel="stylesheet">
        {% endblock %}
    </head>
    <body>
        {% for flash in app.flashes('success') %}
            <div class="alert alert-success">{{ flash }}</div>
        {% endfor %}
        {% for flash in app.flashes('error') %}
            <div class="alert alert-danger">{{ flash }}</div>
        {% endfor %}
        {# Inclusion du lien vers le Header #}
        {{ include ('header.html.twig') }}
        {# Inclusion du lien vers la Navbar #}
        {{ include ('navbar.html.twig') }}
        {# Mise en page Bootstrap avec la classe 'container' #}
        <main class="container">
            {% block body %}{% endblock %}
        </main>
        {# Inclusion du lien vers la Navbar du bas #}
        {{ include ('navbottom.html.twig') }}

        {% block javascripts %}
            {# Intégration fichier JavaScript #}
            <script src="{{ asset('js/script.js') }}"></script>
            {# Intégration fichier Bootstrap JS Bundle #}
            <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-ka7Sk0Gln4gmtz2MlQnikT1wXgYsOg+OMhuP+IlRH9sENBO0LRn5q+8nbTov4+1p" crossorigin="anonymous"></script>
        {% endblock %}
    </body>
</html>