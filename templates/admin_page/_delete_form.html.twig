<form method="post" action="{{ path('admin_page_delete', {'id': page.id}) }}" onsubmit="return confirm('Êtes-vous sûr(e) de supprimer cette entrée ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ page.id) }}">
    <button class="btn">Supprimer</button>
</form>