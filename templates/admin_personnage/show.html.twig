{% extends 'base.html.twig' %}

{% block title %}ADMINISTRATION : Personnage{% endblock %}

{% block body %}
    <h1>{{ personnage.nom }}</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ personnage.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ personnage.nom }}</td>
            </tr>
            <tr>
                <th>Présentation</th>
                <td>{{ personnage.presentation }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ personnage.image }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_personnage_index') }}">Retour à la liste</a>

    <a href="{{ path('admin_personnage_edit', {'id': personnage.id}) }}">Modifier</a>

    {{ include('admin_personnage/_delete_form.html.twig') }}
{% endblock %}