{% extends 'base.html.twig' %}

{% block title %}Personnage{% endblock %}

{% block body %}
    <h1>Personnage</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ personnage.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ personnage.nom }}</td>
            </tr>
            <tr>
                <th>Presentation</th>
                <td>{{ personnage.presentation }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ personnage.image }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_personnage_index') }}">back to list</a>

    <a href="{{ path('admin_personnage_edit', {'id': personnage.id}) }}">edit</a>

    {{ include('admin_personnage/_delete_form.html.twig') }}
{% endblock %}
