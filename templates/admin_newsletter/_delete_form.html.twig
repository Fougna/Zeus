<form method="post" action="{{ path('admin_newsletter_delete', {'id': newsletter.id}) }}" onsubmit="return confirm('Êtes-vous sûr(e) de supprimer cette entrée ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ newsletter.id) }}">
    <button class="btn">Supprimer</button>
</form>