{% extends 'base.html.twig' %}

{% block title %}ZEUS EX MACHINA : Newsletter{% endblock %}

{% block body %}
    <h1>Newsletter</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>N°</th>
                <td>{{ newsletter.id }}</td>
            </tr>
            <tr>
                <th>Prénom</th>
                <td>{{ newsletter.prenom }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ newsletter.nom }}</td>
            </tr>
            <tr>
                <th>E-Mail</th>
                <td>{{ newsletter.mail }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_newsletter_index') }}">Retour à la liste</a>

    <a href="{{ path('admin_newsletter_edit', {'id': newsletter.id}) }}">Modifier</a>

    {{ include('admin_newsletter/_delete_form.html.twig') }}
{% endblock %}