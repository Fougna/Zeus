{% extends 'base.html.twig' %}

{% block title %}ADMINISTRATION : Catégorie d'Article{% endblock %}

{% block body %}
    <h1>{{ categorie_article.nom }}</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>N°</th>
                <td>{{ categorie_article.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ categorie_article.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_categoriearticle_index') }}">Retour à la liste</a>

    <a href="{{ path('admin_categoriearticle_edit', {'id': categorie_article.id}) }}">Modifier</a>

    {{ include('admin_categoriearticle/_delete_form.html.twig') }}
{% endblock %}