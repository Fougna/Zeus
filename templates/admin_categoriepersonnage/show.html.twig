{% extends 'base.html.twig' %}

{% block title %}ADMINISTRATION : Catégorie de Personnage{% endblock %}

{% block body %}
    <h1>{{ categorie_personnage.nom }}</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>N°</th>
                <td>{{ categorie_personnage.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ categorie_personnage.nom }}</td>
            </tr>
            <tr>
                <th>Présentation</th>
                <td>{{ categorie_personnage.presentation }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ categorie_personnage.image }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_categoriepersonnage_index') }}">Retour à la liste</a>

    <a href="{{ path('admin_categoriepersonnage_edit', {'id': categorie_personnage.id}) }}">Modifier</a>

    {{ include('admin_categoriepersonnage/_delete_form.html.twig') }}
{% endblock %}