{% extends 'base.html.twig' %}

{% block title %}CategoriePersonnage{% endblock %}

{% block body %}
    <h1>CategoriePersonnage</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ categorie_personnage.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ categorie_personnage.nom }}</td>
            </tr>
            <tr>
                <th>Presentation</th>
                <td>{{ categorie_personnage.presentation }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ categorie_personnage.image }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_categoriepersonnage_index') }}">back to list</a>

    <a href="{{ path('admin_categoriepersonnage_edit', {'id': categorie_personnage.id}) }}">edit</a>

    {{ include('admin_categoriepersonnage/_delete_form.html.twig') }}
{% endblock %}
