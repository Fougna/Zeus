<form method="post" action="{{ path('admin_categoriepersonnage_delete', {'id': categorie_personnage.id}) }}" onsubmit="return confirm('Êtes-vous sûr(e) de supprimer cette entrée ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ categorie_personnage.id) }}">
    <button class="btn">Supprimer</button>
</form>